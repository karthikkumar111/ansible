---
- hosts: all
  remote_user: ansible
  connection: ssh
  become: yes

  tasks:
    - name: installing httpd in my slave servers
      action: dnf name=httpd state=present

    - name: restart httpd
      service: name=httpd state=restarted
~                                        
